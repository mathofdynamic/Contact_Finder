# Existing dependencies from Contact_extractor.py
selenium>=4.12.0
beautifulsoup4>=4.12.0
requests>=2.31.0
python-dotenv>=1.0.0
gunicorn>=21.0.0
playwright>=1.40.0
gspread>=5.1.0
oauth2client>=4.1.0
# Pillow>=10.0.1  # Skip for now due to build issues
# pytesseract>=0.3.10  # Skip for now

# Additional dependencies for web interface
Flask>=2.3.0
Flask-SocketIO>=5.3.0
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.0
Werkzeug>=2.3.0
eventlet>=0.33.0
python-socketio>=5.9.0

# Optional: For better performance
# gevent>=23.9.0  # Skip for now to avoid conflicts
# gevent-websocket>=0.10.0